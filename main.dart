import 'package:flutter/material.dart';

void main() {
  runApp(hi());
}

class hi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // ignore: todo
    // TODO: implement build
    return MaterialApp(
        home: Scaffold(
      body: Text("Hello hammad"),
    )
        // ignore: dead_code
        );
  }
}
